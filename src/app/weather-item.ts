export class WeatherItem {
  constructor(public city: string, public description: string, public temperature: number, public humidity: number, public pressure: number, public country: string,public icon:string) { }
}
